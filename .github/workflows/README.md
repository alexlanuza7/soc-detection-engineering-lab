# Detection as Code workflows

GitHub Actions workflows for validating detection content will be added here later.

Planned checks may include YAML syntax, Sigma validation and repository structure checks.

Do not add automated checks until the corresponding detection content exists and you understand what the workflow validates.
